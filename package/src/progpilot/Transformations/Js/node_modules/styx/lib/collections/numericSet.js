exports.NumericSet = {
create: createSet
};
function createSet() {
	var lookup = {};
	return {
add: function (value) {
	     lookup[value] = true;
     },
contains: function (value) {
		  return lookup.hasOwnProperty(value.toString());
	  }
	};
}
