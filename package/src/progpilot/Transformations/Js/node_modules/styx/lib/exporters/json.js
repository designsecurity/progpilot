var object_1 = require("./object");
var REPLACER = null;
var INDENTATION_STRING = "  ";
function exportAsJson(flowProgram, _a)
{
    var _b = (_a === void 0 ? { pretty: true } : _a).pretty, pretty = _b === void 0 ? true : _b;
    var exportedObject = object_1.exportAsObject(flowProgram);
    return pretty
        ? JSON.stringify(exportedObject, REPLACER, INDENTATION_STRING)
        : JSON.stringify(exportedObject);
}
exports.exportAsJson = exportAsJson;
